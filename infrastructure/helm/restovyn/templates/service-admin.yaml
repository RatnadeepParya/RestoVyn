apiVersion: v1
kind: Service
metadata:
  name: {{ include "restovyn.fullname" . }}-admin-web
  labels:
    {{- include "restovyn.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin-web
spec:
  type: {{ .Values.adminWeb.service.type }}
  ports:
    - port: {{ .Values.adminWeb.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restovyn.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: admin-web
